using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Game : MonoBehaviour {

    //public GameObject dot;

	void Start () {
        /*
        for (int i = 0; i < 100; ++i)
        {
            Vector3 position = new Vector3(
                Random.Range(0f, Assets.Scripts.GameConstans.MAX_MAP_X),
                Random.Range(0f, Assets.Scripts.GameConstans.MAX_MAP_Y),
                0f);

            //Instantiate(dot, position, Quaternion.identity, transform);
            GameObject d = Instantiate(dot, transform, false);
            d.transform.localPosition = position;

        }
        */
	}
	
	void Update () {
		
	}
}
